import { AppPaper } from '@app/ui';
import { Box } from '@mui/material';

export function PlayerPageResult() {
    return (
        <AppPaper>
            <Box minWidth="50rem" flexGrow={1}></Box>
        </AppPaper>
    );
}
